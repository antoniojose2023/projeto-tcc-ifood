<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppIfoodTcc" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/vermellhoClaro</item>
        <item name="colorPrimaryDark">@color/vermellho</item>
        <item name="android:statusBarColor">@color/branco</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/branco</item>
    </style>

    <style name="Theme.AppIfoodTcc" parent="Base.Theme.AppIfoodTcc" />

</resources>